<%= form_for @experience, :url => user_experience_path(@user, @experience), :html => { :method => :patch } do |f| %>

  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= f.submit "Save Change" %>
<% end %>

<%= link_to 'Exit w.o. Saving', edit1_user_path(@user) %>