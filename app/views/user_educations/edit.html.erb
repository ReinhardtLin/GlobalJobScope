<%= form_for @education, :url => user_education_path(@user, @education), :html => { :method => :patch } do |f| %>


  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= f.submit "Save Change" %>
<% end %>

<%= link_to 'Exit w.o. Saving', edit_user_path(@user) %>